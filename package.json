{
  "name": "spotify-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite serve ./src",
    "build": "vite build",
    "preview": "vite preview", 
    "start": "vite serve ./src --port 3000 --open --host"
  },
  "devDependencies": {
    "vite": "^3.2.0"
  }
}